<div class="show-wrap">
  <div class="wrap">

    <div class="container clearfix">
      <!-- <a href="/featureds"><img class="go_back" src="<%=asset_path 'go_back.png' %>" alt="Go bact to home" /></a> -->
      <button onclick="goBack()"><img class="go_back" src="<%=asset_path 'go_back.png' %>" alt="back" /></button>
      <script>
        function goBack() {
          window.history.back();
        }
      </script>

      <div class="image-box featured-img-box  clearfix">
        <img src="<%= @featured.f_image %>" alt="<%= @featured.f_title %>" title="<%= @featured.f_title %>" />
      </div>

      <div class="inner clearfix">
        <p class="f_title line-break-01"><%= @featured.f_title %></p>
        
        <div class="detail_info clearfix">
          <div class="info-box">
            <p>평점</p>
            <% if @featured.f_spare_01.present? %>
              <span><%= @featured.f_spare_01 %> / 5.0</span>
            <% else %>
              <span> - </span>
            <% end %>
          </div>
          <div class="info-box">
            <p>리뷰</p>
            <% if @featured.f_spare_02.present? %>
              <span><%= @featured.f_spare_02 %> +</span>
            <% else %>
              <span> - </span>
            <% end %>
          </div>
        </div>
        <div class="info-box-price">
          <p>가격</p>
          <% if @featured.f_was_price.present? %>
            <span class="was"><%= number_to_currency(@featured.f_was_price, locale: :kr) %>원</span>
          <% end %>
          <span class="is"><%= number_to_currency(@featured.f_is_price, locale: :kr) %>원</span>
        </div>

      
        <!-- Button Section -->
        <div class="btn">
          <% if @featured.f_source.present? %>
            <a class="amz_btn" target="_blank" href="<%= @featured.f_link %>"><%= @featured.f_source %>에서 구매하기</a>
          <% else %>
            <a class="amz_btn" target="_blank" href="<%= @featured.f_link %>">쿠팡에서 구매하기</a>
          <% end %>
          <% if current_user && current_user.admin %>
            <p class="hashtag"><%= render_with_fhashtags(@featured.f_hashtag) %></p>
            <div class="admin">
              <%= link_to 'Back', featureds_path %> | 
              <%= link_to 'Edit', edit_featured_path(@featured) %> |
              <%= link_to 'Destroy', @featured, method: :delete, data: { confirm: 'Are you sure?' } %>
            </div>
          <% end %>
        </div>

        <div class="notice">
          <span>- 상품 가격은 포스팅 이후 변경 될 수 있습니다.</span>
          <% if @featured.f_source.present? %>
          <% else %>
            <span>- 파트너스 활동을 통해 일정의 수수료를 받을 수 있습니다.</span>
          <% end %>
          
          <!-- <span></span> -->
        </div>

      </div><!--./inner-->
    </div><!--./container-->

  </div>
</div>





