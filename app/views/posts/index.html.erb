<%= render 'shared/header' %>
<%= render 'posts/visual'%>

<div class="contents-box">
  <div class="wrap">

    <div class="ctn-card cb_01 clearfix">
      <h2>Today's Picks of Hotdeals</h2>
      <div class="product-border clearfix">
        <% @sales.each do |sale| %>
          <div class="card">
            <div class="image-box">
              <!-- <a href="/sales/<%= sale.id %>" data-turbolinks="false"><img src="<%= sale.s_image %>" alt="<%= sale.s_title %>" title="<%= sale.s_title %>"></a> -->
              <%= link_to image_tag(sale.s_image, alt: sale.s_title, title: sale.s_title), sale %>
            </div>
            <div class="inner">
              <p class="line-break-01" data-turbolinks="false"><%= link_to sale.s_title, sale%></p>
              
                <span class="list_price"><%= number_to_currency(sale.s_was_price, locale: :kr) %>원</span>
                <span class="current_price"><%= number_to_currency(sale.s_is_price, locale: :kr) %>원</span>
            </div>
              <% hotdeal_pct = 0 %>
              <% hotdeal_pct =  (sale.s_was_price - sale.s_is_price) / sale.s_was_price * 100 %>
              <div class="pct"><p><%= number_to_percentage(hotdeal_pct, precision: 0) %> off</p></div>
          </div>
        <% end %>
      </div>
    </div>

    <div class="show_more_btn">
      <a href="/sales">View More</a>
    </div>

    <!-- <div class="ctn-card cb_03 clearfix">
      <h2>Editor's Picks</h2>
      <div class="pick-box clearfix">
        <% @picks.each do |pick| %>
          <a href="/featureds/fhashtag/<%= pick.p_link %>">
            <div class="card">
              <img src="<%= pick.p_image%>" alt="<%= pick.p_title %>" text="<%= pick.p_title %>">
              <div class="inner">
                <h2><%= pick.p_title %></h2>
                <p><%= pick.p_description %></p>
                <% if current_user && current_user.admin %>
                  <%= render_with_phashtags(pick.p_hashtag) %>
                  <%= link_to 'Edit', edit_pick_path(pick) %>
                  <%= link_to 'Destroy', pick, method: :delete, data: { confirm: 'Are you sure?' } %>
                <% end %>
              </div>
            </div>
          </a>
        <% end %>
      </div>
    </div>

    <div class="show_more_btn">
      <a href="/picks">View More</a>
    </div> -->

  </div><!--./contents-box-->

  
  <%= render 'posts/second_section'%>
  

  <div class="footer">
  </div>

</div>